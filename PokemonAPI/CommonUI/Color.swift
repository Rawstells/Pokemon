//
//  Color.swift
//  PokemonAPI
//
//  Created by Joan Narvaez on 4/02/24.
//

import SwiftUI

public extension Color {
    static let white = Color(hex: "FFFFFF") ?? .white
    static let beige = Color(hex: "A8A878") ?? .gray
    static let black = Color(hex: "303943") ?? .black
    static let blue = Color(hex: "429BED") ?? .blue
    static let brown = Color(hex: "B1736C") ?? .brown
    static let darkBrown = Color(hex: "D0795548") ?? .brown
    static let grey = Color(hex: "64303943") ?? .gray
    static let indigo = Color(hex: "6C79DB") ?? .indigo
    static let lightBlue = Color(hex: "7AC7FF") ?? .blue
    static let lightBrown = Color(hex: "CA8179") ?? .brown
    static let whiteGrey = Color(hex: "FDFDFD") ?? .white
    static let lightCyan = Color(hex: "98D8D8") ?? .cyan
    static let lightGreen = Color(hex: "78C850") ?? .green
    static let lighterGrey = Color(hex: "F4F5F4") ?? .gray
    static let lightGrey = Color(hex: "F5F5F5") ?? .gray
    static let lightPink = Color(hex: "EE99AC") ?? .pink
    static let lightPurple = Color(hex: "9F5BBA") ?? .purple
    static let lightRed = Color(hex: "FB6C6C") ?? .red
    static let lightTeal = Color(hex: "48D0B0") ?? .teal
    static let lightYellow = Color(hex: "FFCE4B") ?? .yellow
    static let lilac = Color(hex: "A890F0") ?? .purple
    static let pink = Color(hex: "F85888") ?? .pink
    static let purple = Color(hex: "7C538C") ?? .purple
    static let red = Color(hex: "FA6555") ?? .red
    static let teal = Color(hex: "4FC1A6") ?? .teal
    static let yellow = Color(hex: "F6C747") ?? .yellow
    static let semiGrey = Color(hex: "bababa") ?? .gray
    static let violet = Color(hex: "7038F8") ?? .purple
    static let orange = Color(hex: "FF9D5C") ?? .orange
    static let lightGray: Color = Color(hex: "D9D9D9") ?? .gray
}
